- git switch <branch name> (New command, specific to switch branch)
- git checkout <branch name> (Old command, can do other things than only switching)
- git checkout <commit hash>

- git merge <branch name> (merge two commits pointing branch to one)
- git rebase (changes history- make same base rather than merging)